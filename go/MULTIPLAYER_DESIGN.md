# 东方异界食堂 (Touhou Isekai Izakaya) 联机功能技术设计文档

本文档旨在定义“联机功能”的技术实现方案，允许玩家通过“加入房主故事”的方式进行多人联机互动。

## 1. 系统架构 (Architecture)

联机系统采用 **房主-客机 (Host-Guest)** 架构，通过一个轻量级的 **Go 后端分发器 (Relay Server)** 进行消息中转。

### 1.1 角色定义
*   **房主 (Host)**：
    *   运行完整的游戏逻辑、状态机和本地 SQLite 数据库。
    *   负责最终的剧情裁定和状态更新。
    *   作为“服务器”的角色，通过 Relay Server 向所有客机同步状态。
*   **客机 (Guest)**：
    *   作为“终端”角色，渲染房主分发的游戏状态。
    *   发送操作指令（对话、行动选择）给房主。
    *   通过 API 共享机制参与剧情生成。
*   **分发服务器 (Relay Server)**：
    *   使用 **Go** 编写，部署在云端。
    *   职责：房间管理、WebSocket 消息转发、心跳检测。
    *   **隐私与安全原则**：
        *   **API Key 绝对安全**：API Key 仅存储在玩家本地浏览器中，**永远不会发送给分发服务器**。
        *   **用完即焚**：所有经过后端中转的消息（聊天流、指令、文本流）在转发后立即从服务器内存中清除，不留任何存档。
        *   **零持久化**：后端不挂载任何数据库，不记录房间密码或历史记录。
    *   **开放性**：后端代码完全开源，支持玩家使用自定义云服务器搭建私有联机平台。

## 2. 核心机制 (Core Mechanisms)

### 2.1 跑团决策系统 (TRPG Decision System)
为了保证联机互动的趣味性与叙事连贯性，采用以下决策流：
*   **实时草稿流 (Drafting) ：
    *   在输入框上方，所有玩家都能看到其他玩家正在输入的“动作预览”（比如： 玩家 A 正在尝试 观察四周... ）。这能产生即时的互动感，避免大家各说各的。
*   **多玩家并发输入 (Multi-input)**：
    *   在自由探索阶段，所有玩家都可以输入自己的行动。
    *   房主端会收集所有玩家的输入流。当房主点击“提交”或满足“全员就绪”条件时，将所有输入汇总。
    *   **Prompt 构造示例**：`玩家A尝试去擦桌子；玩家B正试图偷偷喝掉客人的酒。`
*   **角色专属行动 (Character Actions)**：
    *   玩家可以自定义“动作描述”（如 `*优雅地递上茶杯*`），这些描述会直接传递统一给 LLM。
*   **决策冲突仲裁**：
    *   若玩家输入存在逻辑冲突（如 A 要杀掉 NPC，B 要救下 NPC），房主拥有“最终裁定权”，或者触发“掷骰子” (DICE) 机制由系统随机/基于属性判定结果。

### 2.2 战斗系统同步 (Combat Synchronization)
战斗系统保持回合制，但增加多人协作维度：

*   **动态参战与观战模式 (Dynamic Participation & Spectator Mode)**：
    *   **身份定义**：玩家在联机时扮演的是其自定义的“穿越者”或特定身份，而非强制扮演幻想乡原住民。
    *   **参战判定**：每一场战斗有哪些角色参与，由 **LLM #1 (Storyteller)** 根据当前剧情叙述动态判定。
    *   **按需实例化**：
        *   若 LLM 判定玩家 B 的角色参与了战斗，则玩家 B 进入战斗操作界面，获得 AP/MP 使用权。
        *   若玩家 B 的角色未参与（如在后台观战或不在现场），则玩家 B 自动进入**观战模式**，实时查看战斗流同步，但不显示操作面板。
    *   **NPC 控制权（可选）**：房主可以将剧情中参与战斗的友方 NPC 控制权临时移交给处于“观战状态”的客机玩家，以增加其参与感，但这并非强制。
*   **战斗状态同步**：
*   **实时同步流**：
    *   **房主作为计算核心**：所有的 `calculateDamage` 和随机判定（闪避、暴击）仅在房主端执行。
    *   **状态广播**：房主计算完伤害后，将 `DamageResult` 通过 Relay Server 广播。
    *   **动画同步**：客机接收到 `DamageResult` 后，在本地触发对应的立绘抖动、数字漂浮和音效。

### 2.3 API 共享与投票系统
为了平衡联机成本与安全性，引入基于投票的 API 共享机制：
*   **API 存储**：每个玩家在本地浏览器存储自己的 API Key（LocalStorage）。
*   **请求隔离**：当需要生成剧情时，**仅被选中的 API 调用者客户端**直接与 LLM 供应商服务器（如 OpenAI, Anthropic）通信。
*   **流式转发 (Stream Relay)**：调用者获取到 LLM 的文本流后，将文本内容（非 Key）通过 Relay Server 实时转发给房间内所有人。

### 2.2 多视角统一 (Perspective)
*   所有玩家看到的 UI 布局与单机版一致。
*   **视觉差异**：场景中会显示其他玩家的“特殊角色”标识。
*   **输入控制**：通过 `activePlayerId` 仲裁当前允许输入剧情的玩家，或者开启“自由讨论模式”。

## 3. 通信协议 (Protocol)

使用 WebSocket 进行通信，消息格式为 JSON。

### 3.1 基础消息结构
```json
{
  "type": "MESSAGE_TYPE",
  "senderId": "player_uuid",
  "payload": { ... }
}
```

### 3.2 关键指令定义
*   `JOIN_ROOM`: 申请加入房间。
*   `SYNC_STATE`: 房主向客机分发完整的游戏状态（金钱、好感度、当前场景）。
*   `PLAYER_ACTION`: 玩家发送对话或选择。
*   `LLM_STREAM`: 转发 API 调用者的实时文本流。
*   `VOTE_PROPOSAL`: 发起模型切换或剧情走向投票。

## 4. 房间与连接管理 (Room & Connection)

### 4.1 生命周期 (Lifecycle)
*   **房主权威**：房主是房间的核心。若房主掉线或主动关闭游戏，**房间将直接解散**。所有客机玩家将被强制断开连接并返回主菜单。
*   **客机变动**：客机玩家掉线不影响房间运行，房主可继续推进故事。

### 4.2 数据同步与中途加入 (Sync & Hot-join)
*   **初始同步**：客机加入房间时，房主端会将其当前的**核心状态 (State JSON)** 和 **必要的剧情记忆 (Memory Context)** 直接分发给客机。
*   **实时更新**：
    *   每当房主端的 `gameStore` 或 SQLite 发生关键变化（如金钱变动、好感度更新、获得物品），房主将通过 Relay Server 发送增量同步包。
    *   客机收到包后直接应用到本地的内存 Store 中，确保所见即所得。

*   **记忆同步 (Memory Sync)**：采用方案 B。
    *   **房主主导**：房主端运行 LLM #2 生成剧情摘要与记忆片段。
    *   **指令分发**：房主将生成的摘要文本直接通过同步包分发给客机。
    *   **本地入库**：客机仅负责将收到的摘要存入本地 SQLite，无需再次运行 LLM #2，极大降低了客机的性能要求。

### 4.3 联机 UI 架构：双中心设计
为了平衡“高频互动”与“低频配置”，将联机 UI 拆分为两个独立系统：

#### **1. 联机配置中心 (Multiplayer Settings Hub)**
*   **定位**：低频使用。处理连接、房间管理、系统设置。
*   **入口**：顶部工具栏“联机”图标。
*   **标签页**：
    1.  **大厅 (Lobby)**：连接管理、服务器 URL、房间列表、加入/创建房间。
    2.  **房间 (Room)**：成员列表（仅显示头像/名字/延迟）、权限管理（踢人/转让）。
    3.  **设置 (Settings)**：同步频率、投票规则开关。

#### **2. 跑团决策悬浮页 (TRPG Decision Overlay)**
*   **定位**：高频使用。处理游戏内互动、决策、OOC 交流。
*   **入口**：联机模式下，原主界面下方的“输入框+发送按钮”区域会合为一个 **“跑团决策”大按钮**。点击后弹出该悬浮页。
*   **核心模块**：
    1.  **决策区 (Decision Area)**：
        *   **行动输入**：玩家在这里输入自己的行动/对话。
        *   **草稿同步**：实时显示其他玩家正在输入的摘要。
        *   **提交/就绪**：玩家完成输入后点击“就绪”，房主汇总后统一提交给 LLM。
    2.  **互动区 (Interaction Area)**：
        *   **玩家实时状态 (Character Status)**：HP/MP、当前位置、状态标签。
        *   **OOC 聊天框**：戏外交流专用。
        *   **API 能源池**：显示 API 共享状态与投票进度。
    3.  **系统日志**：实时滚动显示系统消息（某人加入、某人投掷了 DICE、某人断连）。

### 4.4 交互逻辑：聊天与决策的分离
*   **主界面 (Main UI)**：保持纯粹的剧情流展示。只有在剧情推进（LLM 返回结果）后，主界面才会更新。
*   **悬浮页 (Overlay)**：所有“产生动作”的过程都在这里发生。这避免了单机模式下输入框一直占用空间的问题，同时也为多人复杂的决策 UI 腾出了空间。
*   **就绪机制 (Ready System)**：
    *   在悬浮页内，每个玩家输入完后点击“提交/就绪”。
    *   当所有活跃玩家都处于“就绪”状态，或者房主强制推进时，汇总 Prompt 发送。
    *   发送成功后，悬浮页自动关闭，回到主界面等待剧情流刷新。

## 5. UI/UX 设计

### 5.1 联机入口与大厅 (Lobby)
*   **入口**：顶部工具栏新增“联机”图标。
*   **联机大厅 (Multiplayer Lobby)**：
    *   **官方/自定义服务器切换**：默认指向官方服务器，支持手动输入自定义服务器 URL。
    *   **房间列表**：显示官方服务器上的“公开”房间，支持通过房间名搜索。
    *   **加入房间**：点击列表房间或手动输入“房间号”加入。支持设置房间进入密码。
*   **创建房间 (Host Setting)**：
    *   **房间属性**：房主可设置房间名为“私密”或“公开”。
    *   **房间号**：公开房间自动生成唯一的数字房间号。

### 5.2 角色初始化 (Character Setup)
*   **身份创建页**：客机玩家在进入联机存档前，必须先创建/设定自己的“身份”（Identity）。
*   **组件复用与适配**：
    *   复用单机模式的“身份创建”组件，但需针对联机环境调整占位符（Placeholder）逻辑。
    *   **占位符兼容性**：确保 LLM 在引用玩家身份时，能正确区分“房主身份”与“客机玩家 A/B 身份”，避免指代混乱。

### 5.3 存档管理
*   **存档管理面页**：增加“个人故事”与“联机房间”标签。
*   **视觉区分**：联机存档显示“房主”标识及参与玩家数，防止误操作覆盖个人单机进度。

## 5. 提示词适配 (Prompt Adaptation)

联机模式下将启用专门的 `MultiplayerPromptTemplate`：
*   **上下文感知**：在 System Prompt 中加入当前房间内所有玩家的角色背景。
*   **对话标注**：每条历史记录均带上玩家 ID，例如 `[玩家:灵梦]: ...`。
*   **指令优化**：要求 AI 在生成回复时，考虑多人的互动，而不仅仅是回应单一玩家。

## 6. Go 后端分发器详细设计 (Relay Server Deep Dive)

为了实现高性能、极轻量且安全的联机体验，分发服务器采用 Go 语言开发，核心设计准则如下：

### 6.1 核心架构：基于管道的“内存中转站”
*   **无状态转发 (Stateless Relay)**：后端不挂载任何数据库，不持久化任何剧情数据。所有消息仅在内存中进行转发，转发完成即销毁。
*   **高性能并发控制**：
    *   **协程模型 (Goroutines)**：为每个 WebSocket 连接开启独立的 `ReadLoop` 和 `WriteLoop` 协程。
    *   **扇出模式 (Fan-out)**：房主发送的消息通过 `Room` 对象的广播通道（Channel）快速推送到所有客机的写缓冲区，确保毫秒级同步延迟。
    *   **极简二进制**：编译后的单一可执行文件仅约 10MB，部署环境无需任何依赖库。

### 6.2 安全性设计：端到端加密 (E2EE) 与隐私
*   **房间密码即密钥**：
    *   玩家创建房间时设置的密码不仅用于校验，还将作为 **AES-GCM** 加密的种子。
    *   **客户端加密**：剧情文本、敏感指令在浏览器端完成加密后再发送给后端。
    *   **后端透明性**：Go 后端仅处理加密后的字节流（Ciphertext），无法解密剧情内容，从物理层面保证了隐私。
*   **连接认证**：加入房间时通过握手协议校验房间 ID 和密码 Hash，防止非法侵入。

### 6.3 容错与自动清理机制
*   **双向心跳检测 (Heartbeat)**：采用标准的 `Ping/Pong` 机制。若连接在 30 秒内无响应，后端主动关闭连接并回收资源。
*   **房主优先原则 (Host-first Cleanup)**：
    *   房主是房间的生命线。若房主主动断连或超时，后端将立即向所有客机广播 `ROOM_CLOSED` 指令，并销毁内存中的 `Room` 对象，严防“僵尸房间”占用资源。
*   **背压保护 (Backpressure Management)**：
    *   若某个客机因网络波动导致写缓冲区堆积超过阈值，后端将主动断开该弱网连接，防止由于单个玩家的网络问题拖慢整个房间的同步速度。

### 6.4 性能指标预估
*   **内存占用**：单个活跃房间约占用 10-20KB 内存。
*   **承载能力**：在 1C1G 的入门级服务器上，理论可支持 **10,000+** 玩家同时在线。

## 7. 数据一致性与并发控制 (Concurrency & Consistency)

为了防止 WebSocket 异步消息与 WASM-SQLite 写入产生竞态冲突，采用以下方案：

### 7.1 单路指令队列 (Single-Way Command Queue)
*   **原理**：房主端（Host）不直接在 WebSocket 回调中修改数据库。所有来自客机的指令和房主本地的操作，统一进入一个 **串行执行队列**。
*   **执行流**：`接收指令 -> 进入队列 -> 逐一弹出 -> 执行逻辑校验 -> 修改内存 Store -> 异步写入 SQLite -> 广播执行结果`。
*   **优势**：彻底避免了多个异步操作同时读写同一行数据库导致的 `SQLITE_BUSY` 或逻辑数据覆盖问题。

### 7.2 读写权限分离
*   **房主 (Host)**：拥有唯一的 **“核心数据库写入权”**。负责所有金钱、数值、好感度的最终判定和持久化。
*   **客机 (Guest)**：
    *   **内存镜像**：客机本地仅维护房主分发的状态镜像（Pinia Store）。
    *   **非核心写入**：客机仅将接收到的“剧情对话”存入本地 SQLite 以供阅读，**绝不修改**涉及游戏逻辑的数值表。

### 7.3 逻辑锁机制 (Logical Locking)
*   在处理复杂的“多人决策”或“战斗回合提交”时，使用逻辑锁（如 `isProcessing: true`）暂时挂起后续指令，直到当前数据库事务提交完成并完成全网广播。

## 8. 技术栈
*   **前端**：Vue 3 + Pinia + Vite
*   **数据库**：SQLite WASM (仅房主写入，客机镜像)
*   **后端**：Go 1.21+ (使用 `gorilla/websocket` 或 `nhooyr.io/websocket`)
*   **部署**：Docker + 极简 Linux 云服务器

---
*注：本技术文档为开发导向，实际实现时可根据性能反馈调整同步频率。*
